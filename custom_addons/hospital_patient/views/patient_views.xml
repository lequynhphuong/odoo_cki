<odoo>
    <record id="view_hospital_patient_tree" model="ir.ui.view">
        <field name="name">hospital.patient.list</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <list string="Danh Sách Bệnh Nhân">
                <field name="name"/>
                <field name="age"/>
                <field name="disease"/>
                <field name="doctor_id"/>
            </list>
        </field>
    </record>

    <record id="view_hospital_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form string="Bệnh Nhân">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="birth_date"/>
                        <field name="age" readonly="1"/>
                        <field name="needs_parent" readonly="1"/>
                        <field name="disease"/>
                        <field name="doctor_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_hospital_patient_list" model="ir.actions.act_window">
        <field name="name">Danh Sách Bệnh Nhân</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_hospital_patient_tree"/>
    </record>
</odoo>
